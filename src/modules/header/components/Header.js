import React from 'react';

import '../styles/Header.css';

const Header = ({ lang, actChangeLanguage }) => {
  return (
    <div style={{backgroundColor: 'grey'}}>
      <h4 className="test">Header</h4>
    </div>
  );
}

export default Header;
